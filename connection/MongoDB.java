package connection;



public class MongoDB {

	
	
	/*
	private static Game documentToGame(Document doc) {
		Game game = new Game();
		Gson gson = new Gson();
		doc.append("_id", doc.getObjectId("_id").toHexString());
		String json = gson.toJson(doc);
		return gson.fromJson(json, Game.class);
	}
	private static Tag documentToTag(Document doc) {
		Tag tag = new Tag();
		Gson gson = new Gson();
		doc.append("_id", doc.getObjectId("_id").toHexString());
		String json = gson.toJson(doc);
		return gson.fromJson(json, Tag.class);
	}
	*/

}

































